import React from 'react';
import Accueil from '../components/Accueil';
import Header from '../components/Header';

const Home = () => {
    return (
        <div>
            <Header />
            <Accueil />
        </div>
    );
};

export default Home;